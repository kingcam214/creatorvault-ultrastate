# PROFILE: Bug Fix

## Scope
You are working on the CreatorVault production codebase.
Fix the described bug. Trace the root cause. Edit only the affected file(s).

## Rules
- Do NOT refactor unrelated code
- Do NOT add new dependencies unless strictly required
- Do NOT touch the homepage, deploy.yml, or workflows
- Make the smallest possible change that fixes the bug
- Report the root cause and exactly what you changed

## Context
- Production branch: `main`
- App root: `/root/creatorvault` on VPS
- Frontend: React + Vite (client/src/)
- Backend: Node.js + TypeScript (server/)
- Database: MySQL via Drizzle ORM
