# Railway Environment Variables
# This file documents required environment variables for Railway deployment

# CRITICAL: Override default pnpm install command to prevent frozen-lockfile errors
RAILPACK_INSTALL_COMMAND=pnpm install --no-frozen-lockfile

# Database (set in Railway dashboard)
# DATABASE_URL=postgresql://...

# Authentication (set in Railway dashboard)
# JWT_SECRET=...
# OAUTH_SERVER_URL=...
# VITE_APP_ID=...

# Stripe (set in Railway dashboard)
# STRIPE_SECRET_KEY=...
# STRIPE_WEBHOOK_SECRET=...
# VITE_STRIPE_PUBLISHABLE_KEY=...

# Bootstrap (set in Railway dashboard)
# SCHEMA_BOOTSTRAP=1
